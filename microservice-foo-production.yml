server:
  port: ${arb.service.port}

arb:
  service:
    name: microservice-foo
    port: 8890
    url: 111111



#   ${arb.service.name}-${arb.service.url}
profile: ${arb.service.name}-${arb.service.url}-${arb.service.name}
